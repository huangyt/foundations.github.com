# 
# This file is generated from acdkmake
# Copyright (C) 1999-2005 by Roger Rene Kommer, artefaktur
# ALL RIGHTS RESERVED
#

# where acdk lives, target where to install
# You may have to change this.
ACDK_HOME=../..
ACDKHOME=$(ACDK_HOME)
.if exists($(ACDKHOME)/acdk_globals.bsd)
.include "$(ACDKHOME)/acdk_globals.bsd"
.endif

# where to store the .o files
OBJECTDIR=../tobj/acdkx_net_ssl/bsd

# where to store the .a, .so and executables

BINDIR?=../../bin
CCOMPILER?=g++ -Wall -Wno-unused
CCOPTS?=$(XCCOPTS) -g -fpic $(CCOPTSX)
CCINCLUDE?=$(XCCINCLUDE)  -I .  -I $(ACDK_OPENSSL_INCLUDE)  -I ../../include  $(CCINCLUDEX)
DEFINES?=$(XDEFINES)  -D_REENTRANT -DIN_ACDKX_NET_SSL_LIB $(DEFINESX)

LINKSO?=g++ -shared 
LINKEXE?=g++ 

LINK?=g++ -shared
SYSLDFLAGS?=-g -fpic

CFLAGSX=
LDFLAGSX = 


# user ld flags
LDFLAGS=$(XLDFLAGS)   $(LDFLAGSX)

SYSLIBRARIES?=$($SYSLIBRARIES) -lc_r -lm $(SYSLIBRARIESX)

ACDKMC?=$(ACDKHOME)/bin/acdkmc

LIBS = $(SYSLIBRARIES) -l$(ACDK_OPENSSL_LIBS) -Wl,-Bdynamic -L$(BINDIR) -lacdk_core -lacdk_net 


SOURCES= 	acdkx/net/ssl/SSLSocket.cpp \
	acdkx/net/ssl/ssl_metainf_base.cpp \

HEADER= 	acdkx/net/ssl/Config.h \
	acdkx/net/ssl/HTTPSUrlStreamHandler.h \
	acdkx/net/ssl/ssl.h \
	acdkx/net/ssl/SSLSocket.h \

OBJECTS= 	../tobj/acdkx_net_ssl/bsd/SSLSocket.o \
	../tobj/acdkx_net_ssl/bsd/ssl_metainf_base.o \


FINALTARGET=../../bin/libacdkx_net_ssl.so

default: outputdirectories $(FINALTARGET)
all: clean depend outputdirectories $(FINALTARGET)

../../bin/libacdkx_net_ssl.so: $(OBJECTS)
	cd ${.CURDIR}; $(LINKSO) $(SYSLDFLAGS) $(LDFLAGS) -o ../../bin/libacdkx_net_ssl.so $(OBJECTS) $(LIBS) $(LDFLAGSX)

DEPSFILE=acdkx_net_ssl.bsd.deps

depend: 
	@cd ${.CURDIR}; \
	echo -n "Making dependencies ... "; \
	  echo "# Dependencies created by make depend:" >$(DEPSFILE); \
	  echo "" >> $(DEPSFILE); \
	  for FILE in $(SOURCES) ; do \
	    BASENAME=`basename $$FILE .cpp`; \
	    echo -n "$(OBJECTDIR)/" >> $(DEPSFILE); \
	    $(CCOMPILER) -M $(CCOPTS) $(CCINCLUDE) $(DEFINES) $(CCOPTSX) $(CFLAGSX) $$FILE >> $(DEPSFILE); \
	  done; \
	  echo Done.

.if exists(./$(DEPSFILE))
.include "./$(DEPSFILE)"
.endif

../tobj/acdkx_net_ssl/bsd/SSLSocket.o: acdkx/net/ssl/SSLSocket.cpp
	cd ${.CURDIR}; $(CCOMPILER) $(CCOPTS) $(CCINCLUDE) $(DEFINES) $(CCOPTSX) $(CFLAGSX) -o ../tobj/acdkx_net_ssl/bsd/SSLSocket.o -c acdkx/net/ssl/SSLSocket.cpp

../tobj/acdkx_net_ssl/bsd/ssl_metainf_base.o: acdkx/net/ssl/ssl_metainf_base.cpp
	cd ${.CURDIR}; $(CCOMPILER) $(CCOPTS) $(CCINCLUDE) $(DEFINES) $(CCOPTSX) $(CFLAGSX) -o ../tobj/acdkx_net_ssl/bsd/ssl_metainf_base.o -c acdkx/net/ssl/ssl_metainf_base.cpp



metainfo:
	cd ${.CURDIR}; $(ACDKMC) acdkx/net/ssl;\


outputdirectories::
	@cd ${.CURDIR}; \
	if test ! -d $(OBJECTDIR); \
  then \
		mkdir -p $(OBJECTDIR); \
	fi; \
	if test ! -d $(BINDIR); \
  then \
		mkdir -p $(BINDIR); \
	fi 	



clean::
	rm -f ../../bin/acdkx_net_ssl.so $(OBJECTS)

  
