# 
# This file is generated from acdkmake
# Copyright (C) 1999-2000 by Roger Rene Kommer, artefaktur
# ALL RIGHTS RESERVED
#

# where acdk lives, target where to install
# You may have to change this.
ACDK_HOME=../../../..

.if exists($(ACDK_HOME)/acdk_globals.bsd)
.include "$(ACDK_HOME)/acdk_globals.bsd"
.endif

# where to store the .o files
OBJECTDIR=./tobj/acdk_net_Test/bsd

# where to store the .a, .so and executables

BINDIR?=../../../../bin
CCOMPILER?=g++ -Wall -Wno-unused
CCOPTS?=$(XCCOPTS) -g -D_REENTRANT $(CCOPTSX)
CCINCLUDE?=$(XCCINCLUDE)  -I ../src  -I ../include  -I ../../../../include  $(CCINCLUDEX)
DEFINES?=$(XDEFINES)  -D_REENTRANT -DACDK_USE_ACDK_LIB -DUSE_ACDK_PERL_LIB -DUSE_ACDK_TOOLS_TESTUNIT_LIB $(DEFINESX)
LINK?=g++
SYSLDFLAGS?=-g

# user ld flags
LDFLAGS=$(XLDFLAGS)   $(LDFLAGSX)

SYSLIBRARIES?=$($SYSLIBRARIES) -lc_r -lm $(SYSLIBRARIESX)

ACDKMC?=$(ACDKHOME)/bin/acdkmc

LIBS = $(SYSLIBRARIES) -Wl,-Bdynamic -L$(BINDIR) -lacdk_core -lacdk_tools_testunit -lacdk_perl


SOURCES= 	acdk_perl_Test.cpp \
	acdk_perl_Basics.cpp \

HEADER= 
OBJECTS= 	./tobj/acdk_net_Test/bsd/acdk_perl_Test.o \
	./tobj/acdk_net_Test/bsd/acdk_perl_Basics.o \


FINALTARGET=../../../../bin/acdk_net_Test

default: outputdirectories $(FINALTARGET)
all: clean depend outputdirectories $(FINALTARGET)

../../../../bin/acdk_net_Test: $(OBJECTS)
	cd ${.CURDIR}; $(LINK) $(SYSLDFLAGS) $(LDFLAGS)   $(OBJECTS) $(LIBS) -o ../../../../bin/acdk_net_Test

DEPSFILE=acdk_net_Test.bsd.deps

depend: 
	@cd ${.CURDIR}; \
	echo -n "Making dependencies ... "; \
	  echo "# Dependencies created by make depend:" >$(DEPSFILE); \
	  echo "" >> $(DEPSFILE); \
	  for FILE in $(SOURCES) ; do \
	    BASENAME=`basename $$FILE .cpp`; \
	    echo -n "$(OBJECTDIR)/" >> $(DEPSFILE); \
	    $(CCOMPILER) -M $(CCOPTS) $(CCINCLUDE) $(DEFINES) $$FILE >> $(DEPSFILE); \
	  done; \
	  echo Done.

.if exists(./$(DEPSFILE))
.include "./$(DEPSFILE)"
.endif

./tobj/acdk_net_Test/bsd/acdk_perl_Test.o: acdk_perl_Test.cpp
	cd ${.CURDIR}; $(CCOMPILER) $(CCOPTS) $(CCINCLUDE) $(DEFINES) -o ./tobj/acdk_net_Test/bsd/acdk_perl_Test.o -c acdk_perl_Test.cpp

./tobj/acdk_net_Test/bsd/acdk_perl_Basics.o: acdk_perl_Basics.cpp
	cd ${.CURDIR}; $(CCOMPILER) $(CCOPTS) $(CCINCLUDE) $(DEFINES) -o ./tobj/acdk_net_Test/bsd/acdk_perl_Basics.o -c acdk_perl_Basics.cpp



metainfo:


outputdirectories::
	@cd ${.CURDIR}; \
	if test ! -d $(OBJECTDIR); \
  then \
		mkdir -p $(OBJECTDIR); \
	fi; \
	if test ! -d $(BINDIR); \
  then \
		mkdir -p $(BINDIR); \
	fi 	



clean::
	rm -f ../../../../bin/acdk_net_Test $(OBJECTS)

  
