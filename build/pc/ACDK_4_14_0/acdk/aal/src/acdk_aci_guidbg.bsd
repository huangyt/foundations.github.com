# 
# This file is generated from acdkmake
# Copyright (C) 1999-2000 by Roger Rene Kommer, artefaktur
# ALL RIGHTS RESERVED
#

# where acdk lives, target where to install
# You may have to change this.
ACDK_HOME=../..
ACDKHOME=$(ACDK_HOME)
.if exists($(ACDKHOME)/acdk_globals.bsd)
.include "$(ACDKHOME)/acdk_globals.bsd"
.endif

# where to store the .o files
OBJECTDIR=../tobj/acdk_aci_guidbg/bsd

# where to store the .a, .so and executables

BINDIR?=../../bin
CCOMPILER?=g++ -Wall -Wno-unused
CCOPTS?=$(XCCOPTS) -g -fpic $(CCOPTSX)
CCINCLUDE?=$(XCCINCLUDE)  -I .  -I ../../include  $(CCINCLUDEX)
DEFINES?=$(XDEFINES)  -D_REENTRANT -DIN_ACDK_ACI_GUIDBG_LIB $(DEFINESX)
LINK?=g++ -shared
SYSLDFLAGS?=-g -fpic

CFLAGSX = $(WX_CFLAGS)
LDFLAGSX = 


# user ld flags
LDFLAGS=$(XLDFLAGS)   $(LDFLAGSX)

SYSLIBRARIES?=$($SYSLIBRARIES) -lc_r -lm $(SYSLIBRARIESX)

ACDKMC?=$(ACDKHOME)/bin/acdkmc

LIBS = $(SYSLIBRARIES) -l$(WX_LIBS) -Wl,-Bdynamic -L$(BINDIR) -lacdk_core -lacdk_cfgscript -lacdk_text -lacdk_aci -lacdk_wx 


SOURCES= 	acdk/aci/guidbg/CodeTreeCtrl.cpp \
	acdk/aci/guidbg/SyntaxTreeCtrl.cpp \

HEADER= 	acdk/aci/guidbg/CodeTreeCtrl.h \
	acdk/aci/guidbg/Config.h \
	acdk/aci/guidbg/guidbg.h \
	acdk/aci/guidbg/SyntaxTreeCtrl.h \

OBJECTS= 	../tobj/acdk_aci_guidbg/bsd/CodeTreeCtrl.o \
	../tobj/acdk_aci_guidbg/bsd/SyntaxTreeCtrl.o \


FINALTARGET=../../bin/libacdk_aci_guidbg.so

default: outputdirectories $(FINALTARGET)
all: clean depend outputdirectories $(FINALTARGET)

../../bin/libacdk_aci_guidbg.so: $(OBJECTS)
	cd ${.CURDIR}; $(LINK) $(SYSLDFLAGS) $(LDFLAGS) -o ../../bin/libacdk_aci_guidbg.so $(OBJECTS) $(LIBS) $(LDFLAGSX)

DEPSFILE=acdk_aci_guidbg.bsd.deps

depend: 
	@cd ${.CURDIR}; \
	echo -n "Making dependencies ... "; \
	  echo "# Dependencies created by make depend:" >$(DEPSFILE); \
	  echo "" >> $(DEPSFILE); \
	  for FILE in $(SOURCES) ; do \
	    BASENAME=`basename $$FILE .cpp`; \
	    echo -n "$(OBJECTDIR)/" >> $(DEPSFILE); \
	    $(CCOMPILER) -M $(CCOPTS) $(CCINCLUDE) $(DEFINES) $(CCOPTSX) $$FILE >> $(DEPSFILE); \
	  done; \
	  echo Done.

.if exists(./$(DEPSFILE))
.include "./$(DEPSFILE)"
.endif

../tobj/acdk_aci_guidbg/bsd/CodeTreeCtrl.o: acdk/aci/guidbg/CodeTreeCtrl.cpp
	cd ${.CURDIR}; $(CCOMPILER) $(CCOPTS) $(CCINCLUDE) $(DEFINES) $(CFLAGSX) -o ../tobj/acdk_aci_guidbg/bsd/CodeTreeCtrl.o -c acdk/aci/guidbg/CodeTreeCtrl.cpp

../tobj/acdk_aci_guidbg/bsd/SyntaxTreeCtrl.o: acdk/aci/guidbg/SyntaxTreeCtrl.cpp
	cd ${.CURDIR}; $(CCOMPILER) $(CCOPTS) $(CCINCLUDE) $(DEFINES) $(CFLAGSX) -o ../tobj/acdk_aci_guidbg/bsd/SyntaxTreeCtrl.o -c acdk/aci/guidbg/SyntaxTreeCtrl.cpp



metainfo:
	cd ${.CURDIR}; $(ACDKMC) acdk/aci/guidbg;\


outputdirectories::
	@cd ${.CURDIR}; \
	if test ! -d $(OBJECTDIR); \
  then \
		mkdir -p $(OBJECTDIR); \
	fi; \
	if test ! -d $(BINDIR); \
  then \
		mkdir -p $(BINDIR); \
	fi 	



clean::
	rm -f ../../bin/acdk_aci_guidbg.so $(OBJECTS)

  
